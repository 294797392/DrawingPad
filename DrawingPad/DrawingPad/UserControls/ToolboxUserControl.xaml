<UserControl x:Class="DrawingPad.UserControls.ToolboxUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DrawingPad.UserControls"
             xmlns:pad="clr-namespace:DrawingPad"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>

        <DataTemplate x:Key="DataTemplateToolboxItem">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="30"/>
                </Grid.RowDefinitions>

                <Image Grid.Row="0" Source="{Binding Path=IconURI}"/>

                <TextBlock Grid.Row="1" Text="{Binding Path=Name}"/>

            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="DataTemplateToolboxGroup">
            <Grid Background="Yellow" HorizontalAlignment="Stretch">
                <TextBlock Text="{Binding Path=Name}"/>
                <Expander Header="{Binding Path=Name}" Background="Green" HorizontalAlignment="Stretch" Visibility="Collapsed">
                    <ListBox Grid.Row="1" Style="{StaticResource ResourceKey=StyleListBox}" Visibility="Collapsed"
                             ItemsSource="{Binding Path=Items}" ItemTemplate="{StaticResource ResourceKey=DataTemplateToolboxItem}"/>
                </Expander>

            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>

        <ListBox ItemsSource="{Binding Source={x:Static pad:PadContext.Context}, Path=GroupList}"
                 ItemTemplate="{StaticResource ResourceKey=DataTemplateToolboxGroup}"
                 Background="Red">
        </ListBox>

    </Grid>

</UserControl>
